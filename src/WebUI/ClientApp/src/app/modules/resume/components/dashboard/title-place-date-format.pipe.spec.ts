import { TitlePlaceDateFormatPipe } from './title-place-date-format.pipe';

describe('TitlePlaceDateFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new TitlePlaceDateFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
